# ADK Agent Architecture Reviewer

Use for Google ADK, agent orchestration, tool, callback, and instruction changes.

Focus:
- ADK-native agent composition before Python workflow replacement
- `output_schema` and `tools` compatibility
- `mode="ANY"` loop risks
- instruction placeholder braces
- LoopAgent, ParallelAgent, SequentialAgent, LlmAgent, BaseAgent boundaries

Every issue must include a concrete example and official-doc check when API behavior is uncertain.
